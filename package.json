{
  "name": "@sharaal/sql-restrict-pg",
  "version": "1.0.0",
  "description": "Restrict `client.query` only handle queries created with the sql tagged template literal.",
  "repository": "sharaal/sql-restrict-pg",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "lint": "standard",
    "lint:fix": "standard --fix",
    "test": "nyc mocha --require intelli-espower-loader --recursive './test/**/*.spec.js'"
  },
  "directories": {
    "test": "test"
  },
  "devDependencies": {
    "espower-loader": "^1.2.2",
    "husky": "^1.3.1",
    "intelli-espower-loader": "^1.0.1",
    "mocha": "^5.2.0",
    "nyc": "^13.3.0",
    "power-assert": "^1.6.1",
    "sinon": "^7.2.5",
    "standard": "^12.0.1"
  },
  "nyc": {
    "include": "index.js",
    "all": true,
    "reporter": [
      "text",
      "text-summary",
      "lcov"
    ],
    "report-dir": "./coverage"
  },
  "standard": {
    "env": [
      "mocha"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint && npm test"
    }
  }
}
